"""
Submission seeding stub (Phase 4).

This module will generate synthetic StudentAssignment + Answer rows with
realistic score distributions once the Assessments models land in
Phase 3. Kept here as a stub so the seeding package layout is stable.
"""

from typing import Dict


def seed_submissions(*args, **kwargs) -> Dict:
    """Phase 4 — not implemented yet."""
    return {'submissions_created': 0, 'note': 'Phase 4 stub'}
